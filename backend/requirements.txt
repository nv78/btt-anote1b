# ── Core ──────────────────────────────────────────────────────────────────
Flask>=2.3,<4
flask-cors>=4.0,<5
Flask-Limiter>=3.5,<4
requests>=2.31,<3
PyJWT>=2.8,<3
cryptography>=42,<44
Authlib>=1.3,<2
python-dotenv>=1.0,<2

# ── Production server ─────────────────────────────────────────────────────
gunicorn>=21,<24

# ── Eval / ML ─────────────────────────────────────────────────────────────
sacrebleu>=2.4,<3
numpy>=1.26,<2
scipy>=1.13,<2
scikit-learn>=1.4,<2
nltk>=3.8,<4
datasets>=2.19,<4

# ── Database ──────────────────────────────────────────────────────────────
mysql-connector-python>=8.0,<10

# ── Docs / linting (dev, also needed for CI mkdocs build) ─────────────────
flasgger>=0.9,<1

# ── Hosted providers / production integrations ───────────────────────────
openai>=1.40,<2
anthropic>=0.34,<1
google-generativeai>=0.7,<1
boto3>=1.34,<2

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=7,<9
pytest-cov>=4,<6

# ── Optional — uncomment when needed ─────────────────────────────────────
# bert-score>=0.3          # BERTScore translation metric
# transformers>=4.44       # HF model runner
# torch>=2.3               # HF model runner (CPU)
# xai-sdk>=0.2             # xAI LLM runner
