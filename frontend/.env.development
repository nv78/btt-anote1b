# Create React App: dev server port (avoid clash with another app on 3000)
PORT=3001
# Flask API — use the same host/port you pass to `export PORT=... python backend/app.py`
REACT_APP_API_BASE=http://127.0.0.1:5001
